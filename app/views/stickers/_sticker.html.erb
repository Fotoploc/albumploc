<div class="sticker__card effect-border-1 padding-01" id="sticker-<%= sticker.id %>" value="<%= sticker.id %>">
  <div class="sticker__card__picture">
    <%= image_tag bucket_url+sticker.picture.path, alt:"Album Image", class:"album__card__picture__img" if sticker.picture.file != nil%>
    <%= image_tag "https://i.imgur.com/removed.png", alt:"Album Image", class:"album__card__picture__img" if sticker.picture == nil%>
  </div>
</div>
