<div class="sticker__card" id="sticker-<%= sticker.id %>" value="<%= sticker.id %>">
  <div class="sticker__card__picture">
    <%= image_tag bucket_url+sticker.picture.path, alt:"Álbum Image", class:"album__card__picture__img"%>
  </div>
    <p class="sticker__card__code"><%= sticker.code %></p>
</div>
