<p style="color: green"><%= notice %></p>

<h1>Figurinhas</h1>

<div id="stickers">
  <% @stickers.each do |sticker| %>
    <%= render sticker %>
    <p>
      <%= link_to "Show this sticker", sticker %>
    </p>
  <% end %>
</div>

<%= link_to "New sticker", new_sticker_path %>
